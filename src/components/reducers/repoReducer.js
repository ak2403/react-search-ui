import axios from 'axios';
const initialState = {
    notification: '',
    lists: ''
};

export default function RepoReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state;
    }
}